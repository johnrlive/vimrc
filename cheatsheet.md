# VIM Mappings Cheat Sheet

## vim-choosewin  
- `-` => indicates vim window

## for fuzzy finder  
- `<ctrl-p>` => runs :Files in vim

## vim-tmux-navigator
- `<ctrl-h>` => Left
- `<ctrl-j>` => Down
- `<ctrl-k>` => Up
- `<ctrl-l>` => Right
- `<ctrl-\>` => Previous split

